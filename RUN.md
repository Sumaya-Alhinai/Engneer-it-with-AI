# دليل التشغيل — أمان AI

## قبل ما تبدئين

تأكدي إن هذي مثبتة:

```bash
docker --version      # Docker Desktop مشغّل
node --version        # v18 أو أحدث
flutter --version     # للتطبيق فقط
```

---

## الخطوة ١ — شغّلي القاعدة والباك-إند و n8n

من مجلد المشروع الرئيسي:

```bash
docker compose up --build
```

أول مرة تاخذ ٣-٥ دقائق (يبني الصورة وينزّل PostgreSQL و n8n).

**علامة النجاح** — لازم تشوفين بالسجلات:
```
✅ تم التحقق من سكيما قاعدة البيانات
🌱 تم إنشاء 4 كود موظف للتجربة المحلية
🔁 عامل إعادة المحاولة يعمل كل 60 ثانية
```

تحققي بمتصفح: <http://localhost:4000> → لازم يرد `{"status":"ok"}`

> ⚠️ راح تشوفين تنبيه `NOTIFY_PROVIDER=console` وتنبيه `REDIS_URL فارغ`.
> **طبيعي بالتطوير** — ما يوقف التشغيل. بيوقفه فقط لو `ENVIRONMENT=production`.

---

## الخطوة ٢ — استوردي وركفلو الوكلاء بـ n8n

n8n يشتغل بحاوية جديدة فاضية، فلازم تستوردين الوركفلو مرة وحدة:

1. افتحي <http://localhost:5678> وأنشئي حساب محلي (أي إيميل/باسورد)
2. **Settings → Credentials → Add** → اختاري `OpenAI` وحطّي مفتاحك
3. **Workflows → Import from File** — استوردي **التسعة ملفات** من `n8n-workflows/`:
   - `Agent1` إلى `Agent8`
   - `Aman_AI_Master_Workflow.json`
4. افتحي كل وركفلو وفعّليه (مفتاح **Active** فوق يمين)

   > الوركفلو الفرعية **لازم** تكون Active، وإلا استدعاء `toolWorkflow` يفشل

5. بالوركفلو الرئيسي، عقدة `HTTP Request` الأخيرة: تأكدي أن ترويسة
   `x-webhook-secret` قيمتها `local_dev_secret_change_me`
   (نفس القيمة بـ `docker-compose.yml`)

---

## الخطوة ٣ — شغّلي داشبورد الموظفين

بنافذة طرفية **جديدة**:

```bash
cd dashboard
cp .env.example .env.local
npm install
npm run dev
```

افتحي <http://localhost:5173>

**أكواد الدخول للتجربة المحلية:**

| الكود | الصلاحية |
|---|---|
| `DEV-ADMIN-7f2a` | مدير عام — يشوف كل الجهات |
| `DEV-POLICE-3k9m` | الشرطة فقط |
| `DEV-AMB-8p4x` | الإسعاف فقط |
| `DEV-CIVIL-2w6z` | الدفاع المدني فقط |

> هذي **للتجربة المحلية فقط**. للنشر الحقيقي ولّدي أكواد عشوائية:
> `python -c "import secrets; print(secrets.token_urlsafe(16))"`

---

## الخطوة ٤ — شغّلي التطبيق

بنافذة طرفية ثالثة:

```bash
cd app
flutter pub get
flutter run -d chrome --dart-define=API_BASE_URL=http://localhost:4000
```

**على محاكي أندرويد** (العنوان يختلف لأن المحاكي شبكة منفصلة):

```bash
flutter run --dart-define=API_BASE_URL=http://10.0.2.2:4000
```

**على جهاز حقيقي بنفس الواي-فاي** — استبدلي بـ IP جهازك:

```bash
ipconfig            # ويندوز — دوّري على IPv4
flutter run --dart-define=API_BASE_URL=http://192.168.1.5:4000
```

---

## الخطوة ٥ — جرّبي المسار كامل

هذي أهم خطوة قبل العرض:

1. **بالتطبيق**: "الدخول كضيف" → أرسلي بلاغًا بصورة وموقع
2. تحققي إنك استلمتِ رقم بلاغ مثل `AMN-K7M2QX9P`
3. **بـ n8n** → تبويب Executions: لازم تشوفين تنفيذًا ناجحًا
4. **بالداشبورد**: البلاغ يظهر بجهته وأولويته ودرجة خطورته
5. غيّري الحالة من الداشبورد → **ارجعي للتطبيق** وتأكدي إن الحالة تحدّثت

**لو البلاغ ظهر بشارة ⚠︎** = الوكلاء ما عالجوه. راجعي:
- الوركفلو Active بـ n8n؟
- مفتاح OpenAI صحيح؟
- سجلات الباك-إند بتقول ليش

العامل الدوري بيعيد المحاولة تلقائيًا كل دقيقة، فلو أصلحتِ السبب بيتعافى وحده.

---

## الخطوة ٦ (اختيارية) — شغّلي الاختبارات

```bash
cd aman-backend
pip install pytest
DATABASE_URL=postgresql://aman:aman_local_dev@localhost:5433/AmanAI pytest tests/ -v
```

٤٠ اختبار، يمشون بثانيتين. أي واحد يفشل = ضابط أمني انكسر.

---

## مشاكل شائعة

| العرض | السبب والحل |
|---|---|
| `port is already allocated` | خدمة تستخدم المنفذ. `docker compose down` ثم أعيدي |
| التطبيق يقول "تعذّر الاتصال" | العنوان غلط للمنصة — راجعي الخطوة ٤ |
| الداشبورد فاضي بعد الدخول | القسم ما عنده بلاغات. جرّبي `DEV-ADMIN-7f2a` |
| CORS error بالمتصفح | الداشبورد على منفذ غير 5173 — أضيفيه لـ `ALLOWED_ORIGINS` |
| رمز التحقق ما وصل | طبيعي — بوضع التطوير يُطبع بسجلات الباك-إند |
| n8n: "workflow not active" | فعّلي **كل** الوركفلو الفرعية لا الرئيسي فقط |

## إيقاف كل شيء

```bash
docker compose down          # يوقف ويبقي البيانات
docker compose down -v       # يحذف البيانات أيضًا (بداية نظيفة)
```
