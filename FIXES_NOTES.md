# ملخص التعديلات — جولة المراجعة الحالية

## 1) رفع الملفات (Flutter) — تم الإصلاح والتحقق منطقيًا

**الملف:** `app/lib/services/reports_service.dart`

- **المشكلة:** `MultipartFile.fromBytes` كان يُستدعى بدون تحديد `contentType`، فترجع مكتبة
  `http` بشكل افتراضي `application/octet-stream` — وهذا يؤثر على **كل رفع ملف** (صور
  وصوت)، وليس الفيديو فقط. الباك-إند يرفض أي نوع محتوى غير مطابق لقائمته البيضاء
  برسالة "نوع ملف غير مدعوم".
- **الحل:** أضفت `_resolveContentType()` تستنتج النوع الصحيح من `XFile.mimeType` أو من
  امتداد اسم الملف، وتمريره صراحة لكل من حقلي `media` و `voice_note`.

**الملف:** `app/lib/screens/emergency_report_screen.dart`

- **المشكلة:** خيار "تسجيل فيديو" بقائمة الإرفاق يستدعي `pickVideo`، لكن الباك-إند
  بأكمله (فحص النوع، EXIF، تمويه الوجوه، تحليل الصورة بالذكاء الاصطناعي عبر n8n)
  مبني للصور فقط ولا يقبل الفيديو إطلاقًا مهما كان `contentType` صحيحًا.
- **الحل:** حذفت الخيار والباراميتر `video` من `_pickMedia` بالكامل.

**الملف:** `app/pubspec.yaml`

- أضفت `http_parser: ^4.0.2` كتبعية مباشرة (كانت متوفرة ضمنيًا فقط عبر `http`).

---

## 2) تزامن وكلاء n8n المستقلة مع Master Workflow — تم إصلاح الأجزاء الآمنة للتعديل

اكتشفت أن ملفات الوكلاء المستقلة (`Agent1..Agent8`) نسخة **قديمة** عن نفس الوكلاء
المدمجين داخل `Aman_AI_Master_Workflow.json`، والفرق بينهم مو شكلي فقط — فيه منطق
أمان ناقص بالنسخ القديمة. زامنت الملفات التالية مع نسخة Master (نص البرومبت،
`systemMessage`، ومخطط `Structured Output Parser`):

- `Agent1_Intake_استقبال_البلاغ.json`
- `Agent2_Classification_تصنيف_البلاغ.json`
- `Agent3_Severity_تحديد_مستوى_الخطورة.json`
- `Agent6_FinalDecision_اتخاذ_القرار.json`

(`Agent4` و `Agent5` كانا متطابقين مع Master أصلًا — ما احتاجوا تعديل.)

**أهم إصلاح وظيفي:** بالنسخة القديمة من Agent3، البرومبت المرسل للنموذج ما كان
يتضمن قيمة `type_match` إطلاقًا رغم إن تعليمات النظام تعتمد عليها لتحديد "هل البلاغ
يحتاج مراجعة يدوية". يعني قاعدة الأمان هذي كانت معطّلة عمليًا بالنسخة المستقلة. بعد
التزامن، البرومبت صار يمرر `type_match` و`confirmed_incident_type` صراحة للنموذج،
مطابقًا لنسخة Master.

كذلك صححت تسمية الحقل بربط الاستدعاء بين Agent2 و Agent3 (كان يُرسل باسم
`selected_incident_type` بينما Agent3 يتوقع `confirmed_incident_type`)، وأضفت تمرير
`type_match` بينهما اللي كان غائبًا تمامًا من الاستدعاء.

---

## ⚠️ ملاحظة مهمة — ما لم يتم إصلاحه (يحتاج مراجعة داخل واجهة n8n نفسها)

أثناء الفحص لاحظت إن بعض ملفات الوكلاء المستقلة (خصوصًا `Agent1` و `Agent6`) فيها
عقد أخرى (مثل `Insert row` بملف Agent1) تشير لحقول لا تطابق أصلًا مخرجات وكيل
الذكاء الاصطناعي في نفس الملف — يعني مشكلة تصميم/توصيل عقد أقدم وأعمق من مجرد
اختلاف نص برومبت. **لم أعدّل هذا الجزء** لأنه يحتاج تجربة فعلية داخل n8n للتأكد من
مسار البيانات الصحيح بين العقد (لا يمكن التحقق منه بأمان بمجرد قراءة ملف JSON بدون
تشغيله).

**التوصية:** افتحوا ملفات `Agent1` و`Agent6` المستقلة داخل محرر n8n، شغّلوها تجريبيًا
بمدخل وهمي (Manual Trigger)، وتابعوا قيمة كل عقدة خطوة بخطوة للتأكد من وصول
الحقول الصحيحة. أو الأفضل: اعتمدوا `Aman_AI_Master_Workflow.json` كمصدر وحيد
للتشغيل الفعلي، ولا تستخدموا الملفات المستقلة إلا كمرجع/نسخ احتياطي، عشان تفادوا
انحراف النسخ عن بعض مستقبلًا.

---

## جولة إضافية — بعد اختبارك الحقيقي على الجهاز

### 🔴 ثغرة خصوصية: صور آيفون كانت تُحفظ بلا تمويه

HEIC هي الصيغة الافتراضية لكاميرا آيفون، وOpenCV لا يكتب ملفًا بامتداد ‎.heic.
السلسلة كانت: التمويه الموجّه يفشل ← التمويه الاحتياطي يفشل للسبب نفسه ←
**الصورة تُحفظ كما هي** ← **الخادم يرد بنجاح**. فشل صامت للخصوصية.

**الإصلاح مزدوج**:
- توحيد كل صورة إلى JPEG قبل المعالجة (`normalize_to_jpeg`) مع نقل EXIF، وإضافة
  `pillow-heif`. كل صورة تمر بمسار واحد مضمون الكتابة.
- **فشل مغلق**: صورة يتعذّر تمويهها لا تُحفظ إطلاقًا، ويُرد بـ 422. رفض بلاغ
  أقل ضررًا من نشر وجوه أشخاص لم يوافقوا.

### 🔴 التطبيق كان يمسح بيانات EXIF قبل الإرسال

`pickImage(imageQuality: 80)` يعيد ضغط الصورة، وإعادة الضغط **تمسح EXIF
بالكامل** — الوقت والجهاز وإحداثيات الكاميرا. أي أن ميزة التحقق من صحة الصور
كانت معطّلة عمليًا من مصدرها، ويظهر دائمًا "لا توجد بيانات وصفية بالصورة".

**الإصلاح**: حذف `imageQuality` والاكتفاء بـ `maxWidth/maxHeight: 2400` — يقلل
الحجم دون مسح البيانات.

### أثر جانبي كشفه الفشل المغلق

`png_bytes` بملف الاختبارات كانت ترجع ترويسة PNG متبوعة بأصفار — بايتات ليست
صورة. مرّت طويلًا لأن فشل المعالجة كان يُتجاهل بصمت. أُصلحت لتولّد صورًا حقيقية.

**الاختبارات: ٥٤** (أُضيفت: تحويل HEIC، الفشل المغلق، نجاة EXIF من المعالجة).

---

## 🔴 خطأ قلب درجة الخطورة (اكتُشف أثناء التجربة الحية)

**العرض**: بلاغ بصورة فواكه ظهر بأولوية "منخفضة" ودرجة خطورة **100%**.

**السبب**: دالة `_normalize_risk_score` كانت تفترض أن أي قيمة بين 0 و1 نسبة
كسرية فتضربها في 100، وأي قيمة بين 1 و10 على قياس عشري فتضربها في 10.
الافتراض جاء من أن الوكلاء القدامى كانوا يخلطون المقاييس.

الوكيل الجديد قيّم الصورة بخطورة **1 من 100** — تقييم صحيح تمامًا — فحوّلته
الدالة إلى **100**.

**لماذا هذا خطير وليس تجميليًا**: بقرار طوارئ، عرض بلاغ لا خطر فيه بأنه أخطر
ما ورد اليوم قد يصرف فرقة عن حادث حقيقي. الخطأ يقلب المعنى رأسًا على عقب.

**الإصلاح**: كل الوكلاء بالمنسّق الجديد يرجعون 0-100 صراحةً بتوجيهاتهم، فصارت
الدالة تقصّ القيمة على المدى فقط دون إعادة قياس. لا تخمين ولا مضاعفة.

**اختبار يحرس ضد التكرار**: تسع حالات تشمل 1 و0.9 و8 — القيم التي كانت تنقلب.

**الاختبارات: 58**.
